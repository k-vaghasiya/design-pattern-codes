public class Main {
    public static void main(String[] args) {
        Tracker tracker = new Tracker();
        tracker.getLocation();
        tracker.getLocation();
        tracker.getLocation();
    }
}